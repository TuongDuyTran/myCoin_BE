import DataTypes from "sequelize";

const {MODEL_NAME} = {
  tableName: "{TABLE_NAME}",
  tableInfo: {
    ID:{
      type: DataTypes.INTEGER,
      autoIncrement: true,
      primaryKey: true,
      allowNull: false
    }
  },
  relationship: function (dbo) {
    /*this.hasMany(dbo.<ModelName>, {
      foreignKey: <ColumnName>
    });
    dbo.<ModelName>.belongsTo(this, {
      foreignKey: <ColumnName>
    });*/
  }
};